---
trigger: always_on
---

# Git Workflow Rule

1. Always merge changes from `dev` into `main` automatically upon completing changes.
2. After merging, always push `main` to remote via `git push origin main` (and `git push origin dev`).
